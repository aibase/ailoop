export default function (context) {
  // To run Cogito's combinatorial / cross-ref / click / blockchain / ranking functions 
  // To Do: separate folder / route for 'market' beside about/admin/posts !!!
  // To 'return' if function is asynchronous
  // runs on server or client depending if it is initial load or already loaded app
  // only works on page components / no effect on other components
  console.log('[Middleware] The Log Middleware is running')
}